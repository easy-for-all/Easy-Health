COLE NO SEU CLAUDE DO VSCODE (Claude Code), na raiz do projeto.
Anexe TODOS os arquivos deste pacote.

OBJETIVO: portar o DESIGN COMPLETO do protótipo EasyHealth para o app real,
não só a tela de treinos. A fundação (tokens Lumen) já está no globals.css —
então é portar layouts/telas reusando os tokens existentes. Faça por ondas, na
ordem abaixo, validando cada uma antes da próxima. NÃO altere a experiência de
Musculação (deve ficar idêntica).

Leia 00-DESIGN-COMPLETO.md (tem o mapa tela-a-tela e o passo a passo). Resumo:

ONDA 1 — Engines de execução (corrige o bug + telas ricas)
- Crie src/features/workout/workout-engine.ts (conteúdo enviado).
- app/workout/today/page.tsx: importe workoutEngine; troque isCardio/isTimed
  pelos helpers novos; adicione isInterval. (PATCH.md)
- Copie workout-engine-screens.tsx para app/workout/today/ e, na fase
  "exercising", troque os ramos isTimed/isCardio pelos <RecoveryPanel/>,
  <IntervalPanel/>, <CardioPanel/> com as props do PATCH-design-fiel.md.
  NÃO mexa no ramo de FORÇA nem no cabeçalho/cronômetro.

ONDA 2 — Picker de modalidade agrupado
- Copie modality-picker.tsx para app/workout/quick/.
- No passo 1 do app/workout/quick/page.tsx, substitua a lista plana
  (MODALITY_OPTIONS → SelectCard) por <ModalityPicker value onSelect/>.

ONDA 3 — Ambiente + Equipamentos + Ramificações
- Copie environment-step.tsx para app/workout/quick/.
- No passo 3, substitua o grid de LOCATION_OPTIONS por <EnvironmentStep/>
  (ver props no 00-DESIGN-COMPLETO.md). Adicione estados equipment e
  branchAnswer. Inclua equipment no POST de generate().

ONDA 4 — Gerenciar treino agrupado + Treinos (lista)
- Implemente os SPECs 7 e 8 do 00-DESIGN-COMPLETO.md usando o protótipo
  EasyHealth Pro.html como referência visual (cards de ciclo editáveis;
  hero "Treino de hoje"; histórico).

REGRAS:
- Use os tokens do globals.css e os componentes existentes. Sem libs novas.
- Cada componente já traz fallback de token onde precisa.
- Ao fim de cada onda, me diga os arquivos alterados e cole o trecho-chave
  para eu conferir, e me mande um print.

VALIDE no fim:
- Quick: passo 1 agrupado; Bike+Casa pergunta esteira e sugere troca.
- Execução: Bike/HIIT/Mobilidade com painéis ricos; Musculação intacta.
- Gerenciar treino de cardio = ciclos agrupados.
